.DS_Store

# Source turntable render — 1.2 GB ProRes, far past GitHub's 100 MB per-file
# hard limit. Keep it local; the iframe uses the .glb instead.
*.mov

# editor swap files
*.swp
*.swo
*~

# Uncompressed Blender export (28 MB). The deployed model is the
# meshopt-compressed roman.glb; keep this locally as the source.
iframe/Roman/Large new RST_Model_V004.glb
